apiVersion: v2
name: docker-scs-1-33-cluster-class
description: |
  SCS Cluster Class (1.33) for the Docker infrastructure provider.
  Uses CAPI v1beta1 APIs with camelCase variables (imageRepository, certSANs, oidcConfig).
type: application
version: v1
